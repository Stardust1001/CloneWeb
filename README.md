# CloneWeb
A website downloader written with Node.js, it's faster

# How to use it ?
1. npm install
2. In common.js, set 'site' to the website you wish to download, and set 'host_pattern' to the domain allowed for related downloads.
3. 'node main.js' to run
